server:
  port: ${PORT:8095}
spring:
  application:
    name: passport
  cloud:
    config:
      uri: ${CONFIG_URI:http://localhost:8762}
      fail-fast: true
      label: master
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
