server:
  port: ${PORT:8084}
spring:
  application:
    name: bank
  cloud:
    config:
      uri: ${CONFIG_URI:http://localhost:8762}
      fail-fast: true
      label: master
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}

